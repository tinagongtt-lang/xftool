name: Python package

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.10"]

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        python -m pip install build setuptools wheel
    - name: 打包构建
      run: python -m build
    - name: 纯净安装测试
      run: python -m pip install .
    - name: 基础逻辑验证
      run: |
        python -c "import xftool; print('🛸 xftool 核心代码在 ${{ matrix.os }} 校验通过！')"
