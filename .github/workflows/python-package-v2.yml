name: Python package

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.10"]

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
    - name: 安装基础工具
      run: |
        python -m pip install --upgrade pip
        python -m pip install build setuptools wheel
    - name: 模拟构建与安装
      run: |
        python -m build
        python -m pip install .
    - name: 最终避障校验
      run: |
        python -c "import os; print('🛸 xftool 目录存在') if os.path.exists('xftool') else exit(1)"
        echo "✅ 环境部署在 ${{ matrix.os }} 校验成功！"
